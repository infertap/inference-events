[package]
name = "inference-events"
version = "1.0.0"
edition = "2021"
rust-version = "1.85"
license = "Apache-2.0"
readme = "README.md"
description = "Typed wire records and validation for inference telemetry"
repository = "https://github.com/infertap/inference-events"
authors = ["InferTap <cort@infertap.ai>"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
